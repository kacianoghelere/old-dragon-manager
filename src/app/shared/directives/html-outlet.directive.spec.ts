import { HtmlOutletDirective } from './html-outlet.directive';

describe('HtmlOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlOutletDirective();
    expect(directive).toBeTruthy();
  });
});
