import { WikiLinkDirective } from './wiki-link.directive';

describe('WikiLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new WikiLinkDirective();
    expect(directive).toBeTruthy();
  });
});
